# Охоронна консоль на ESP32-S3

Це модіфікація орігінального проекту https://gitlab.com/prime72w/arming_console

## Що додано / змінено

- додано LED який симулює стан базера (off/on)
- прибрано варіант з реле для чистоти коду (використовується зумер через транзистор)
- додано підтримку PULLDOWN і PULLUP кнопок (в моєму випадку 2 кнопки pull_down і 2 кнопки pull_up)
- зберігаємо стан кнопок у flash памʼяті (считуємо при ініціалізації і зберігаємо при зміні стану)
- створюємо таймер для кнопок (тік ~10 мс) в `main.c` 

### Компоненти прошивки

- `components/button_fsm` — антидребезгова обробка кнопок (short/long press) на періодичному `esp_timer` (тік ~10 мс), а також обробка PANIC-переривання.
- `components/load_driver` — драйвер зумера/сирени (підключення зумера через транзисторний ключ BC547B з PWM-регулюванням гучності).
- `components/pot_sensor` — читання потенціометра через ADC (12-біт, канал ADC1_CH9), використовується для розрахунку часу затримки постановки на охорону.
- `components/nvs_storage` - запис і считування стану кнопок "zones" (bypass and trip) у flash
- `main/main.c` — головний FSM системи, обробники подій кнопок, задачі зумера (чирп-сигнали через чергу та переривчастий писк під час ARMING).

## Схема на макетній платі

![Схема ESP32-S3](esp32-s3-schema-photo.heic)
